# A LAN host without machine learning: the mobile app talks to the server directly, and
# the library sits on a second disk.
HOST_BIND=0.0.0.0
HOST_PORT=2283
HOST_LIBRARY_DIR=/srv/photos/immich
HOST_POSTGRES_DIR=%h/.local/share/immich/postgres
TZ=Europe/Berlin
IMMICH_MACHINE_LEARNING_ENABLED=false
